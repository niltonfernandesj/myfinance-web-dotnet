@model PlanoContaModel 

@{
    ViewData["Title"] = "Cadastro de plano de contas";
}

<h1>@ViewData["Title"]</h1>

<form asp-controller="PlanoConta" asp-action="Cadastro" method="post">
    <div class="container">
        <div class="mb-3">
            <label class="form-label">Descrição</label>
            <input type="hidden" id="txtId" class="form-control" asp-for="Id"/>
            <input type="text" id="txtNome" class="form-control" asp-for="Descricao"/>
        </div>
         <div class="mb-3">
            <label>@Html.RadioButtonFor(m => m.Tipo, "D")Despesa</label>
            <label>@Html.RadioButtonFor(m => m.Tipo, "R")Receita</label>
        </div>
        <div class="mb-3">
            <button type="submit" class="btn btn-success">Confirmar</button>
            <button type="button" class="btn btn-danger" onclick="goBack()">Cancelar</button>
        </div>
    </div>
</form>




<script>
    function goBack() {
        window.location.href = window.location.protocol + "//" + window.location.host + "/PlanoConta/Index"
    }
</script>